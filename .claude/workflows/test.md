# TEST
- Run the smallest failing target first.
- Prefer package-local typecheck/test before root-wide commands.
- Separate environment failures from code failures.
- Android compilation may only be claimed when Gradle/SDK/JDK tooling actually runs.
