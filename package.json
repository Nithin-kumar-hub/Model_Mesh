{
  "name": "neuralforge",
  "version": "0.1.0",
  "private": true,
  "description": "Neural Forge — AI Workload Planner & Orchestrator",
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "prisma": "pnpm --filter @neuralforge/api exec prisma",
    "seed": "pnpm --filter @neuralforge/api run seed",
    "test-providers": "pnpm --filter @neuralforge/api run test-providers"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "~5.4.5"
  }
}
