<?xml version="1.0" encoding="utf-8"?>
<!--
  Referenced by the frozen manifest (android:networkSecurityConfig). Without it,
  every debug request to the cleartext dev backend on 10.0.2.2 dies with
  "CLEARTEXT communication not permitted" (HANDOFF §10.4).

  Cleartext is permitted ONLY for the dev backend hosts:
    - 10.0.2.2  : the Android emulator's alias for the host machine's loopback
    - localhost / 127.0.0.1 : a device running the backend locally / adb reverse
  Everything else still requires TLS via the base-config.

  If a physical device points at a LAN IP (e.g. 192.168.x.x), add that IP as
  another <domain> here — see TRACK-B-REPORT.md.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
