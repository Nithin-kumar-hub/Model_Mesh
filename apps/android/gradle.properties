org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# The API base URL and key the debug build talks to. Override in
# ~/.gradle/gradle.properties or on the command line; never commit a real key.
MODELMESH_API_BASE_URL=http://10.0.2.2:3000/api/v1/
MODELMESH_WS_BASE_URL=http://10.0.2.2:3000
MODELMESH_API_KEY=dev-secret-change-me
