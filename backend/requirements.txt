# ModelMesh backend — runtime dependencies
# Install on a machine with internet: pip install -r requirements.txt
# NOTE: the routing core is pure-stdlib; these deps are only for the API edge
# (FastAPI/Pydantic) and the real provider adapter (LiteLLM).
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3.0
httpx>=0.27
PyYAML>=6.0
# Unified real-provider adapter layer (Groq, OpenRouter, and more)
litellm>=1.40,<2.0
